# Dados brutos

Aqui ficarão os dados coletados diretamente da webcam (landmarks do MediaPipe).

> Os arquivos de dados (.npy, .csv) não são versionados no Git por serem grandes.
> Use o script `scripts/coletar_dados.py` para gravar novos dados.
